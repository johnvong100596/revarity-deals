# Build Phase — Revarity Marketing Engine (standalone module)

**Current phase:** see this repo's own **`PLAN.md`** — it is the sequenced implementation plan and the authoritative task list for this module. The six-stone phase model does not apply here.

> This file exists only so `revarity-context` resolves. For real status, read `PLAN.md`, `DECISIONS.md`, and the README in the repo root.

## Notes
Two open decisions gate downstream work and need Malcolm: D-01 (pricing model) and D-02 (SaaS brand separation). Until resolved: pricing-agnostic copy with `[PENDING-D01]` token; STR-operator surfaces only (no SaaS-facing surface).
